[project]
name = "terminal-tool"
version = "0.1.0"
description = "Terminal Tool MCP Server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.3",
    "httpx>=0.28.1",
    "mcp[cli]>=1.25.0",
]
